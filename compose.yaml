services:
  mc:
    image: mc-image
    environment:
      EULA: "true"
    ports:
      - "8888:25565"
    volumes:
      - data:/data
    stdin_open: true
    tty: true
    restart: unless-stopped
volumes:
  data: {}